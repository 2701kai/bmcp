{
  "name": "bmcp",
  "version": "0.2.0",
  "description": "BEVMAQ MCP server: catalogue, knowledge, repos and deploy state for every Claude surface",
  "author": "2701kai",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "stdio": "bun src/stdio.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "reindex": "bun src/knowledge.ts --reindex"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "hono": "^4.13.7",
    "minisearch": "^7.2.0",
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
